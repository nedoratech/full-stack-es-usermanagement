namespace UserRegistration.UserManagement;

internal static class ErrorCodes
{
    internal static class Keys
    {
        internal const string CloudEvent = nameof(CloudEvent);
        internal const string Aggregate = nameof(Aggregate);
    }

    internal static class Codes
    {
        internal const string Invalid =  nameof(Invalid);
    }
}